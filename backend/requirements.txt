# Django
Django>=5.0,<6.0
djangorestframework>=3.15,<4.0
djangorestframework-simplejwt>=5.3,<6.0
django-cors-headers>=4.3,<5.0
python-decouple>=3.8,<4.0

# Celery & Redis
celery>=5.3,<6.0
redis>=5.0,<6.0

# Real-time notifications (WebSocket)
channels>=4.1,<5.0
channels-redis>=4.2,<5.0
daphne>=4.1,<5.0

# Notification rendering & delivery
Jinja2>=3.1,<4.0

# HTTP clients (payment gateways, notification providers)
requests>=2.31,<3.0

# AWS S3 storage (production)
django-storages>=1.14,<2.0
boto3>=1.34,<2.0

# Production server
gunicorn>=22.0,<24.0
